<?xml version="1.0" encoding="UTF-8"?>
<!-- Application context definition for zgdr Datasource. -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="jedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
        <property name="maxActive" value="50"/>
        <property name="maxIdle" value="10"/>
        <property name="maxWait" value="1000"/>
        <property name="testOnBorrow" value="true"/>
    </bean>

    <bean id="jedis.shardInfo" class="redis.clients.jedis.JedisShardInfo">
        <constructor-arg index="0" value="${passport.redis.url}"/>
        <constructor-arg index="1" value="${passport.redis.port}"/>
    </bean>

    <bean id="shardedJedisPool" class="redis.clients.jedis.ShardedJedisPool">
        <constructor-arg index="0" ref="jedisPoolConfig"/>
        <constructor-arg index="1">
            <list>
                <ref bean="jedis.shardInfo"/>
            </list>
        </constructor-arg>
    </bean>

    <!--  <bean id="jedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
          <property name="maxActive" value="50"/>
          <property name="maxIdle" value="10"/>
          <property name="maxWait" value="1000"/>
          <property name="testOnBorrow" value="true"/>
      </bean>

      <bean id="jedisConnectionFactory" class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory"-->
          <!--p:usePool="true">-->
        <!--<property name="hostName" value="${passport.redis.url}"/>-->
        <!--<property name="port" value="${passport.redis.port}"/>-->
        <!--&lt;!&ndash;<property name="password" value="${redis.pass}"/>&ndash;&gt;-->
        <!--&lt;!&ndash;<property name="timeout" value="${redis.timeout}"/>&ndash;&gt;-->
        <!--&lt;!&ndash;<property name="database" value="${redis.default.db}"></property>&ndash;&gt;-->
        <!--<constructor-arg index="0" ref="jedisPoolConfig"/>-->
    <!--</bean>-->

    <!--&lt;!&ndash; redis template definition &ndash;&gt;-->
    <!--<bean id="redisTemplate" class="org.springframework.data.redis.core.StringRedisTemplate"-->
          <!--p:connectionFactory-ref="jedisConnectionFactory"/>-->
</beans>
